"""Reference oracles for vernier subsystems.

A "reference oracle" is a pure-Python (numpy) implementation of a vernier
capability, written for readability and held as the spec the Rust
implementation is validated against. Each oracle's directory carries its
own ADR pointer in its package docstring.
"""
