# vernier-mask (name reservation)

Placeholder v0.0.0 release that claims the `vernier-mask` name on
crates.io. The real crate — RLE encode/decode and rasterization helpers
that mirror pycocotools' mask API — will land at v0.0.1 or later;
the project ships under 0.0.x patches until the core and extended
feature set is complete. Track <https://github.com/NoeFontana/vernier>.

License: MIT OR Apache-2.0.
