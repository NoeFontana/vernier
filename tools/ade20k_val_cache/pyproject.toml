[project]
name = "vernier-ade20k-val-cache"
version = "0.0.0"
description = "Single source of truth for the ADE20K SceneParse150 val dev cache contract."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT OR Apache-2.0" }

[build-system]
requires = ["hatchling>=1.18"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["ade20k_val_cache"]
